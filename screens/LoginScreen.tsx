import { Text, View } from 'react-native';

const LoginScreen = () => {
  return (
    <View className="flex-1">
      <Text className="mt-150 text-3xl text-white">login screen</Text>
    </View>
  );
};

export default LoginScreen;

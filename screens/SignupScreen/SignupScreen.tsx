import { ScrollView, View } from 'react-native';

const SignupScreen = () => {
  return (
    <ScrollView keyboardDismissMode="on-drag">
      <View style={{ gap: 16, padding: 16, marginBottom: '20%' }}></View>;
    </ScrollView>
  );
};

export default SignupScreen;

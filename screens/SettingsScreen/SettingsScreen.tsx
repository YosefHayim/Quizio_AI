import { SafeAreaView, Text } from 'react-native';

const SettingsScreen = () => {
  return (
    <SafeAreaView className="flex-1">
      <Text className="text-white">SettingsScreen screen</Text>
    </SafeAreaView>
  );
};

export default SettingsScreen;

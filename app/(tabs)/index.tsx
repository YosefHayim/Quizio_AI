import { Text, View } from "react-native";

const HomeScreen = () => {
  return (
    <View style={{ flex: 1, backgroundColor: "white" }}>
      <Text style={{ fontSize: 24, marginTop: 100 }}>Welcome back! Ready to learn something new today?</Text>
    </View>
  );
};

export default HomeScreen;
